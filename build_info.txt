Local Build

cmake --build build
ctest --test-dir build --output-on-failure


intended container workflow:
docker compose --profile skeleton build

Docker image installs its own compiler and Ninja.